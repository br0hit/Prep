#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve()
{
    vector<pair<int,vector<int>>> test;
    vector<int> temp(3,3);
    test.push_back({1,temp});

    cout<<test[0].second[0];
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    // int t; cin>>t; while(t--)
    solve();
    return 0;
}